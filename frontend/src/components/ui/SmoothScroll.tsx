/**
 * Placeholder: Lenis was removed — it called preventDefault on wheel events, which
 * caused sluggish page scroll and broke native wheel scrolling inside textareas
 * and other nested scrollers (especially on /studio). The document now uses
 * native browser scrolling everywhere.
 */
export function SmoothScroll() {
  return null;
}
