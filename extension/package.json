{
  "name": "vellum-extension",
  "version": "2.0.0",
  "description": "Chrome MV3 extension that detects invisible Unicode watermarks in web page text.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsc -p . --outDir dist && cp manifest.json dist/ && cp popup/popup.html popup/popup.css dist/popup/ && cp -R icons dist/icons",
    "check": "tsc -p . --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "@types/chrome": "^0.0.270"
  }
}
