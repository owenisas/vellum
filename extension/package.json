{
  "name": "veritext-extension",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.js",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.281",
    "esbuild": "^0.24.0",
    "typescript": "~5.9.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "canonicalize": "^2.0.0"
  }
}
