[project]
name = "vellum-watermark"
version = "2.0.0"
description = "Invisible Unicode watermark library for AI-generated text."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Vellum", email = "team@vellum.io" }]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
